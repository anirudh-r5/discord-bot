# discord-bot
A Discord bot written in JavaScript

**NOTE:** Create a *key.json* file in the root folder and store the bot's API key as a *token* variable

Example:

`{
	"token": "your-token-goes-here"
}`